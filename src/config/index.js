import {colors} from './colors';
import {
  iconSize,
  margin,
  padding,
  lineHeight,
  borderRadius,
  inputHeight,
  borderSize,
} from './spacing';
import {sizes, lineHeights} from './fonts';
import {DEVICE_WIDTH, DEVICE_HEIGHT, IS_ANDROID} from './statics';

export {
  colors,
  iconSize,
  inputHeight,
  margin,
  padding,
  lineHeight,
  borderRadius,
  borderSize,
  sizes,
  lineHeights,
  DEVICE_WIDTH,
  DEVICE_HEIGHT,
  IS_ANDROID,
};
